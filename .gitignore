dev/.stabilize-build.lock

# 高負荷テストの可視化用 Python 仮想環境（run-load.sh / plot 用）
dev/.venv/

# Python キャッシュ
dev/jenkins-env/lib/__pycache__/

# start.sh がデプロイしたコミットを記録する（レポートの provenance 用・実行時生成）
dev/jenkins-env/.deployed-plugin

# --debug 実行のレポート（再現不能・保持ポリシー対象外）
dev/reports/debug/
