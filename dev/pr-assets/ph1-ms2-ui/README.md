# PR assets — Phase 1 M2+M3 UI tour

Screenshots linked from the pull request. They live here rather than under `dev/reports/` because
that directory keeps only the latest report of each kind, and a rotated-away file would turn a link
in someone else's pull request into a 404.

Link them by commit hash, never by branch:

    https://raw.githubusercontent.com/kohtaro-satoh/lockable-resources-remote-notes/<sha>/dev/pr-assets/ph1-ms2-ui/<file>

Captured on plugin `111a767` against the four-controller harness in `dev/jenkins-env/`:
A and C borrow, B and D lend. `dev/reports/*-ui-capture.md` holds the scripted before/after set,
which is a different thing - these are the hand-taken tour of the finished UI.
